name: Test Setup Script

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run setup script (dry-run)
        run: bash scripts/common/setup-common-stack.sh --dry-run
